from django.apps import AppConfig


class CouponsConfig(AppConfig):
    """Application configuration for the coupons domain."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.coupons"