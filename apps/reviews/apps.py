from django.apps import AppConfig


class ReviewsConfig(AppConfig):
    """Application configuration for the reviews domain."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.reviews"